'use strict';

/**
  On récupère ces préférences n'importe où dans l'application
  à l'aide de :
      Prefs.get('<id preference>')
  Par exemple :
      Prefs.get('lastSiteChecked')
**/
Prefs.app_data = {
  // preference_id: <valeur par défaut>
  lastSiteChecked: null
}
